let num=[1,2,4,5,6]
let b=num.toString()
console.log(b)
let c=num.join("B \n") //join each element by B and return that array
console.log(c)
let r=num.pop( 4) //pop the last element and return that array
console.log(num,r)
let p=num.push(34) //push the given element inside the paranthesis and return that array
console.log(num,p)
let s=num.shift() //Reove first element from array
console.log(s)
let u=num.unshift(45)//It adds to the first array and replace by given number inside the paranthesis
console.log(u,num)